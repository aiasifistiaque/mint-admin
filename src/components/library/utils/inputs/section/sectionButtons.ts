import { ButtonProps } from '@chakra-ui/react';

/** The footer buttons of the section modals — a notch smaller than the usual size sm. */
export const SECTION_BUTTON: ButtonProps = { size: 'xs', h: '28px', px: 3 };
